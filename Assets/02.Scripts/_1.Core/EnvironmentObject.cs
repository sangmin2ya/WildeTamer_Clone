using UnityEngine;

namespace WildTamer
{
    /// <summary>
    /// 돌, 나무 등 환경 오브젝트를 식별하는 컴포넌트입니다.
    /// 깊이 정렬은 Project Settings → Graphics의 Custom Sort Axis(0, 1, 0)가 자동으로 처리합니다.
    /// </summary>
    public class EnvironmentObject : MonoBehaviour { }
}
